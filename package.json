{
  "name": "OMCL",
  "version": "0.1.0",
  "description": "Omni Minecraft Launcher",
  "main": "main/main.js",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "build": "node scripts/build.js && npm run generate-icons && electron-builder",
    "build:win": "node scripts/build.js && npm run generate-icons && electron-builder --win",
    "build:mac": "node scripts/build.js && npm run generate-icons && electron-builder --mac",
    "build:linux": "node scripts/build.js && npm run generate-icons && electron-builder --linux",
    "generate-icons": "electron-icon-builder --input=./resources/OMCL.png --output=./build/icons --flatten"
  },
  "repository": "https://github.com/OMCLauncher/OMCL",
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.4.1",
    "chalk": "^4.1.2",
    "chokidar": "^3.5.3",
    "electron": "^32.1.2",
    "electron-builder": "^25.1.6",
    "typescript": "^5.2.2",
    "vite": "^4.5.0"
  },
  "dependencies": {
    "electron-icon-builder": "^2.0.1",
    "vue": "^3.3.8"
  }
}
